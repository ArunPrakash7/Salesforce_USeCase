public static void updateCaseStatus(List<Id> caseIds, String newStatus) {
        List<Case> casesToUpdate = [SELECT Id FROM Case WHERE Id IN :caseIds];
        
        for (Case c : casesToUpdate) {
            c.Status = newStatus;
        }
        
        update casesToUpdate;
    }
}
